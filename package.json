{
	"name": "05-noc",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev-1": "tsnd --respawn --clear src/app.ts",
		
		"dev": "node --env-file=.env -r ts-node/register src/app.ts",
		"dev:watch": "node --env-file=.env --watch -r ts-node/register src/app.ts",
		"build": "rimraf ./dist && tsc",
		"start": "npm run build && node dist/app.js"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"devDependencies": {
		"@types/node": "25.5.0",
		"rimraf": "6.1.3",
		"ts-node-dev": "2.0.0",
		"typescript": "5.9.3"
	},
	"dependencies": {
		"cron": "4.4.0",
		"env-var": "7.5.0"
	}
}
